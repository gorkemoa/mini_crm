// Remote sync service skeleton — reserved for future backend integration.

class RemoteSyncService {
  Future<void> pushAll(List<Map<String, dynamic>> items) async {
    throw UnimplementedError('Remote sync not configured yet.');
  }

  Future<List<Map<String, dynamic>>> pullAll() async {
    throw UnimplementedError('Remote sync not configured yet.');
  }
}

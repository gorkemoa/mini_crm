enum ClientStatus { active, inactive, archived }

enum DebtStatus { pending, overdue, paid, partial }

enum ProjectStatus { planned, startingSoon, active, paused, completed, cancelled }

enum LeadStage { newLead, contacted, proposalSent, negotiating, won, lost }

enum ReminderRelatedType { client, debt, project, lead, income, general }

enum AppThemeMode { light, dark, system }
